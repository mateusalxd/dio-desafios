interface Driver {
    id: number,
    id_team: number,
    name: string,
}

interface DriverDto {
    id: number,
    driver_name: string,
    team_name: string,
};

interface DriverParams {
    id: string,
}